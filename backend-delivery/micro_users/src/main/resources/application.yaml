spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/users_db
    username: postgres
    password: Haikyuu1212
    driver-class-name: org.postgresql.Driver

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/ceiba-bar
          jwk-set-uri: http://localhost:8080/realms/ceiba-bar/protocol/openid-connect/certs

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate.format_sql: true